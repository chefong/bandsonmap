import React from 'react';
import { Marker } from 'react-map-gl';
import './Marks.css';

const musicIcon = require('../assets/imgs/music-player.png');

const Marks = props => {
  { console.log(props.artistEvents) }
  return (null)
}

export default Marks;